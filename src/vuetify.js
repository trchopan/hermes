import { VApp, VBtn, VCard, VBottomNav, VIcon, transitions } from "vuetify";
import { Ripple } from "vuetify/es5/directives";

export const components = {
  VApp,
  VBtn,
  VCard,
  VIcon,
  transitions
};
export const directives = {
  Ripple
};
